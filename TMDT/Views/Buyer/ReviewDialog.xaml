<Window x:Class="TMDT.Views.Buyer.ReviewDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Đánh giá sản phẩm"
        Height="450" Width="480"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        MouseDown="Window_MouseDown">

    <Border Background="White" CornerRadius="16" Margin="8">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" Color="#00000022" ShadowDepth="4" Opacity="1"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" CornerRadius="16,16,0,0" Background="#1E1B4B" Padding="24,20">
                <Grid>
                    <StackPanel>
                        <TextBlock Text="Đánh giá sản phẩm" FontSize="18" FontWeight="Bold"
                                   Foreground="White" FontFamily="Segoe UI"/>
                        <TextBlock x:Name="ProductNameText" Text="Tên sản phẩm..."
                                   FontSize="12" Foreground="#94A3B8" Margin="0,4,0,0" 
                                   FontFamily="Segoe UI" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" VerticalAlignment="Top"
                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                            Click="CloseButton_Click" ToolTip="Đóng">
                        <TextBlock Text="&#xE711;" FontFamily="Segoe MDL2 Assets"
                                   FontSize="12" Foreground="#94A3B8"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Form -->
            <StackPanel Grid.Row="1" Margin="24,20,24,0">
                <!-- Stars -->
                <TextBlock Text="Chất lượng sản phẩm" FontSize="13" FontWeight="Medium" Foreground="#374151" HorizontalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,20">
                    <Button x:Name="Star1" Style="{StaticResource MaterialDesignIconForegroundButton}" Width="40" Height="40" Padding="0" Margin="4" Click="Star_Click" Tag="1">
                        <materialDesign:PackIcon Kind="Star" Width="35" Height="35" Foreground="#FBBF24"/>
                    </Button>
                    <Button x:Name="Star2" Style="{StaticResource MaterialDesignIconForegroundButton}" Width="40" Height="40" Padding="0" Margin="4" Click="Star_Click" Tag="2">
                        <materialDesign:PackIcon Kind="Star" Width="35" Height="35" Foreground="#FBBF24"/>
                    </Button>
                    <Button x:Name="Star3" Style="{StaticResource MaterialDesignIconForegroundButton}" Width="40" Height="40" Padding="0" Margin="4" Click="Star_Click" Tag="3">
                        <materialDesign:PackIcon Kind="Star" Width="35" Height="35" Foreground="#FBBF24"/>
                    </Button>
                    <Button x:Name="Star4" Style="{StaticResource MaterialDesignIconForegroundButton}" Width="40" Height="40" Padding="0" Margin="4" Click="Star_Click" Tag="4">
                        <materialDesign:PackIcon Kind="Star" Width="35" Height="35" Foreground="#FBBF24"/>
                    </Button>
                    <Button x:Name="Star5" Style="{StaticResource MaterialDesignIconForegroundButton}" Width="40" Height="40" Padding="0" Margin="4" Click="Star_Click" Tag="5">
                        <materialDesign:PackIcon Kind="Star" Width="35" Height="35" Foreground="#FBBF24"/>
                    </Button>
                </StackPanel>

                <!-- Review Content -->
                <TextBlock Text="Nội dung đánh giá" FontSize="12.5" FontWeight="Medium" Foreground="#374151" Margin="0,0,0,6"/>
                <Border Background="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="8" Padding="12" Height="100">
                    <TextBox x:Name="ReviewContentBox"
                             materialDesign:HintAssist.Hint="Hãy chia sẻ nhận xét của bạn về sản phẩm này nhé..."
                             BorderThickness="0" Background="Transparent"
                             FontSize="13" FontFamily="Segoe UI"
                             TextWrapping="Wrap" AcceptsReturn="True"
                             VerticalContentAlignment="Top" Height="76"/>
                </Border>
            </StackPanel>

            <!-- Footer Buttons -->
            <Border Grid.Row="2" CornerRadius="0,0,16,16" Padding="24,16" Background="#F9FAFB">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" Height="40" Background="#F3F4F6"
                            Foreground="#374151" BorderThickness="0" Cursor="Hand"
                            Click="CancelButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="8">
                                    <TextBlock Text="Hủy bỏ" HorizontalAlignment="Center"
                                               VerticalAlignment="Center" FontSize="13" FontWeight="SemiBold"
                                               FontFamily="Segoe UI"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bd" Property="Background" Value="#E5E7EB"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button Grid.Column="2" Height="40" Width="140" Background="#0D9488"
                            Foreground="White" BorderThickness="0" Cursor="Hand"
                            Click="SubmitButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="8">
                                    <TextBlock Text="Gửi đánh giá" HorizontalAlignment="Center"
                                               VerticalAlignment="Center" FontSize="13" FontWeight="SemiBold"
                                               FontFamily="Segoe UI"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bd" Property="Background" Value="#0F766E"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
