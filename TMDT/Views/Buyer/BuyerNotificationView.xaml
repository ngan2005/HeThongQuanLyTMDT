<UserControl x:Class="TMDT.Views.Buyer.BuyerNotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TMDT.Views.Buyer"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:TMDT.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="{StaticResource BackgroundBrush}">
    
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <converters:InverseBooleanToVisibilityConverter x:Key="BoolToVisInverse"/>
        <converters:ProductCountToItemsVisibilityConverter x:Key="ItemsVis"/>
        
        <SolidColorBrush x:Key="Primary" Color="#5221E7"/>
        <SolidColorBrush x:Key="Surface" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextMain" Color="#1A1B22"/>
        <SolidColorBrush x:Key="TextMuted" Color="#797588"/>
        <SolidColorBrush x:Key="UnreadBg" Color="#F3F0FF"/>
    </UserControl.Resources>

    <Grid Margin="40,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Thông báo của bạn" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextMain}" VerticalAlignment="Center"/>
            <Button HorizontalAlignment="Right" Background="Transparent" BorderThickness="0" Cursor="Hand" 
                    Command="{Binding MarkAllAsReadCommand}" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="CheckAll" Foreground="{StaticResource Primary}" Width="20" Height="20" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Đánh dấu tất cả đã đọc" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource Primary}" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Empty state -->
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center"
                    Visibility="{Binding Notifications.Count, Converter={StaticResource ItemsVis}, ConverterParameter=Inverse}">
            <materialDesign:PackIcon Kind="BellOffOutline" Width="80" Height="80" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Center" Margin="0,0,0,16"/>
            <TextBlock Text="Chưa có thông báo nào" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource TextMain}" HorizontalAlignment="Center" Margin="0,0,0,8"/>
            <TextBlock Text="Các thông báo về đơn hàng và khuyến mãi sẽ xuất hiện ở đây." FontSize="14" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Notifications List -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                      Visibility="{Binding Notifications.Count, Converter={StaticResource ItemsVis}}">
            <ItemsControl ItemsSource="{Binding Notifications}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border CornerRadius="12" Margin="0,0,0,16" Padding="20" Cursor="Hand"
                                Background="{Binding IsRead, Converter={StaticResource BoolToVis}, ConverterParameter=UnreadBg}">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="{StaticResource Surface}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsRead}" Value="False">
                                            <Setter Property="Background" Value="{StaticResource UnreadBg}"/>
                                        </DataTrigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#F8F9FA"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Border.InputBindings>
                                <MouseBinding Gesture="LeftClick" Command="{Binding DataContext.MarkAsReadCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}"/>
                            </Border.InputBindings>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Icon -->
                                <Border Grid.Column="0" Width="48" Height="48" CornerRadius="24" Background="#E5E0FA" Margin="0,0,16,0" VerticalAlignment="Top">
                                    <materialDesign:PackIcon Kind="BellOutline" Foreground="{StaticResource Primary}" Width="24" Height="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>

                                <!-- Content -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Title}" FontSize="16" Foreground="{StaticResource TextMain}" Margin="0,0,0,4">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsRead}" Value="False">
                                                        <Setter Property="FontWeight" Value="Bold"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBlock Text="{Binding Content}" FontSize="14" Foreground="{StaticResource TextMuted}" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding CreatedAt, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}" FontSize="12" Foreground="{StaticResource TextMuted}" Margin="0,8,0,0"/>
                                </StackPanel>

                                <!-- Unread dot -->
                                <Ellipse Grid.Column="2" Width="10" Height="10" Fill="{StaticResource Primary}" HorizontalAlignment="Right" VerticalAlignment="Center"
                                         Visibility="{Binding IsRead, Converter={StaticResource BoolToVisInverse}}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
