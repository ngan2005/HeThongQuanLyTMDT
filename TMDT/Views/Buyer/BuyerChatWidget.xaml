<UserControl x:Class="TMDT.Views.Buyer.BuyerChatWidget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <DropShadowEffect x:Key="WidgetShadow" BlurRadius="20" ShadowDepth="2" Opacity="0.1" Color="#000" Direction="270"/>

        <!-- ScrollViewer Style -->
        <Style x:Key="ChatScroll" TargetType="ScrollViewer">
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid>
                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"/>
                            <ScrollBar x:Name="PART_VerticalScrollBar" HorizontalAlignment="Right"
                                       Value="{TemplateBinding VerticalOffset}" Maximum="{TemplateBinding ScrollableHeight}"
                                       ViewportSize="{TemplateBinding ViewportHeight}" Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                       Width="4" Opacity="0.5" Background="Transparent"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,30,30">
        <!-- Chat Window -->
        <Border x:Name="ChatWindow" Width="700" Height="500" Background="White" 
                CornerRadius="16" BorderThickness="1" BorderBrush="#E2E8F0"
                Effect="{StaticResource WidgetShadow}" Margin="0,0,0,80"
                Visibility="{Binding IsOpen, Converter={StaticResource BoolToVis}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="260"/> <!-- Contact List -->
                    <ColumnDefinition Width="1"/>   <!-- Divider -->
                    <ColumnDefinition Width="*"/>   <!-- Chat Area -->
                </Grid.ColumnDefinitions>

                <!-- LEFT PANE: CONTACTS -->
                <Grid Grid.Column="0" Background="#F8FAFC">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Contacts Header -->
                    <Border Grid.Row="0" Padding="16,20,16,16" BorderThickness="0,0,0,1" BorderBrush="#E2E8F0">
                        <StackPanel>
                            <TextBlock Text="Tin nhắn" FontSize="18" FontWeight="Bold" Foreground="#1E293B" FontFamily="Segoe UI"/>
                        </StackPanel>
                    </Border>

                    <!-- Contact List -->
                    <ListBox Grid.Row="1" ItemsSource="{Binding Contacts}" SelectedItem="{Binding SelectedContact}"
                             Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="16,12"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#F1F5F9"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter Property="Background" Value="#E2E8F0"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="40"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Width="40" Height="40" CornerRadius="20" Background="#EEF2FF">
                                        <TextBlock Text="{Binding LogoChar}" FontWeight="Bold" FontSize="16" Foreground="#4F46E5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                                        <Grid>
                                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14" Foreground="#1E293B" FontFamily="Segoe UI"/>
                                            <TextBlock Text="{Binding TimeDisplay}" FontSize="11" Foreground="#64748B" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                        </Grid>
                                        <TextBlock Text="{Binding LastMessage}" FontSize="12" Foreground="#64748B" FontFamily="Segoe UI" TextTrimming="CharacterEllipsis" MaxWidth="170" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>

                <Border Grid.Column="1" Background="#E2E8F0"/>

                <!-- RIGHT PANE: ACTIVE CHAT -->
                <Grid Grid.Column="2" Background="White">
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedContact}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Hidden"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Chat Header -->
                    <Border Grid.Row="0" Background="White" Padding="20,16" BorderThickness="0,0,0,1" BorderBrush="#E2E8F0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <Border Width="40" Height="40" CornerRadius="20" Background="#EEF2FF">
                                <TextBlock Text="{Binding SelectedContact.LogoChar}" FontWeight="Bold" FontSize="16" Foreground="#4F46E5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            
                            <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding SelectedContact.Name}" FontSize="16" FontWeight="Bold" Foreground="#1E293B" FontFamily="Segoe UI"/>
                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                    <Ellipse Width="8" Height="8" Fill="#10B981" Margin="0,0,6,0"/>
                                    <TextBlock Text="Đang hoạt động" FontSize="12" Foreground="#10B981" FontFamily="Segoe UI"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <Button Grid.Column="2" Command="{Binding ToggleChatCommand}" Background="Transparent" BorderThickness="0" Padding="8" Cursor="Hand" Foreground="#64748B">
                                <materialDesign:PackIcon Kind="Close" Width="20" Height="20"/>
                            </Button>
                        </Grid>
                    </Border>

                    <!-- Chat Messages -->
                    <ScrollViewer x:Name="MessagesScrollViewer" Grid.Row="1" Style="{StaticResource ChatScroll}" Padding="20,10">
                        <ItemsControl ItemsSource="{Binding Messages}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,8">
                                        <!-- Mine (Buyer) -->
                                        <StackPanel HorizontalAlignment="Right" Visibility="{Binding IsMine, Converter={StaticResource BoolToVis}}">
                                            <Border Background="#4F46E5" CornerRadius="16,16,4,16" Padding="14,10" MaxWidth="300">
                                                <TextBlock Text="{Binding Content}" Foreground="White" FontSize="14" FontFamily="Segoe UI" TextWrapping="Wrap"/>
                                            </Border>
                                            <TextBlock Text="{Binding TimeDisplay}" FontSize="11" Foreground="#94A3B8" HorizontalAlignment="Right" Margin="0,6,6,0"/>
                                        </StackPanel>
                                        
                                        <!-- Theirs (Shop) -->
                                        <StackPanel HorizontalAlignment="Left">
                                            <StackPanel.Style>
                                                <Style TargetType="StackPanel">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsMine}" Value="True">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </StackPanel.Style>
                                            <Border Background="#F1F5F9" CornerRadius="16,16,16,4" Padding="14,10" MaxWidth="300">
                                                <TextBlock Text="{Binding Content}" Foreground="#1E293B" FontSize="14" FontFamily="Segoe UI" TextWrapping="Wrap"/>
                                            </Border>
                                            <TextBlock Text="{Binding TimeDisplay}" FontSize="11" Foreground="#94A3B8" HorizontalAlignment="Left" Margin="6,6,0,0"/>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <!-- Message Input Box -->
                    <Border Grid.Row="2" Background="White" Padding="16" BorderThickness="0,1,0,0" BorderBrush="#E2E8F0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <Border Grid.Column="0" Background="#F1F5F9" CornerRadius="24" Margin="0,0,12,0">
                                <TextBox Text="{Binding MessageText, UpdateSourceTrigger=PropertyChanged}" 
                                         Background="Transparent" BorderThickness="0" Padding="16,12"
                                         FontSize="14" FontFamily="Segoe UI" Foreground="#1E293B"
                                         CaretBrush="#4F46E5"
                                         materialDesign:HintAssist.Hint="Nhập tin nhắn..."
                                         VerticalAlignment="Center">
                                    <TextBox.InputBindings>
                                        <KeyBinding Key="Enter" Command="{Binding SendMessageCommand}"/>
                                    </TextBox.InputBindings>
                                </TextBox>
                            </Border>

                            <Button Grid.Column="1" Command="{Binding SendMessageCommand}" Width="48" Height="48" Background="#4F46E5" BorderThickness="0" Foreground="White" Cursor="Hand" Style="{StaticResource MaterialDesignFloatingActionMiniButton}">
                                <materialDesign:PackIcon Kind="Send" Width="20" Height="20" Margin="4,0,0,0"/>
                            </Button>
                        </Grid>
                    </Border>
                </Grid>

                <!-- Initial Empty State -->
                <Grid Grid.Column="1" Grid.ColumnSpan="2" Background="White">
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedContact}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,-20,0,0">
                        <materialDesign:PackIcon Kind="ChatProcessingOutline" Width="100" Height="100" Foreground="#E2E8F0" HorizontalAlignment="Center" Margin="0,0,0,24"/>
                        <TextBlock Text="Chat với Cửa hàng" FontSize="20" FontWeight="Bold" Foreground="#1E293B" FontFamily="Segoe UI" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                        <TextBlock Text="Chọn một cuộc trò chuyện để bắt đầu" FontSize="14" Foreground="#64748B" FontFamily="Segoe UI" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <!-- Floating Button -->
        <Button Command="{Binding ToggleChatCommand}" Width="64" Height="64" 
                Background="#4F46E5" BorderThickness="0" Foreground="White"
                Cursor="Hand" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                Style="{StaticResource MaterialDesignFloatingActionButton}">
            <Button.Effect>
                <DropShadowEffect BlurRadius="16" ShadowDepth="4" Opacity="0.3" Color="#4F46E5"/>
            </Button.Effect>
            <Grid>
                <materialDesign:PackIcon Kind="Chat" Width="32" Height="32" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <materialDesign:PackIcon.Style>
                        <Style TargetType="materialDesign:PackIcon">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsOpen}" Value="True">
                                    <Setter Property="Kind" Value="Close"/>
                                    <Setter Property="Width" Value="28"/>
                                    <Setter Property="Height" Value="28"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </materialDesign:PackIcon.Style>
                </materialDesign:PackIcon>
            </Grid>
        </Button>
    </Grid>
</UserControl>
