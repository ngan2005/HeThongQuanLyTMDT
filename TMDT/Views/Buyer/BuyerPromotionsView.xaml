<UserControl x:Class="TMDT.Views.Buyer.BuyerPromotionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:TMDT.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="{StaticResource BackgroundBrush}">

    <UserControl.Resources>
        <!-- Converts null list count to visibility -->
        <converters:CountToVisibilityConverter x:Key="CountToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" CornerRadius="12" Padding="25" Margin="0,0,0,20" Effect="{StaticResource CardShadow}">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="TicketPercentOutline" Width="36" Height="36" Foreground="{StaticResource BrandPrimaryBrush}" VerticalAlignment="Center"/>
                <StackPanel Margin="15,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="KHO VOUCHER KHUYẾN MÃI" FontSize="24" FontWeight="Bold" Foreground="{StaticResource BrandPrimaryBrush}"/>
                    <TextBlock Text="Tổng hợp các mã giảm giá, Freeship cực hot dành cho bạn." FontSize="14" Foreground="{StaticResource TextMutedBrush}" Margin="0,5,0,0"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <!-- Empty State -->
                <StackPanel Visibility="{Binding Vouchers.Count, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=Empty}" 
                            HorizontalAlignment="Center" Margin="0,50,0,0">
                    <materialDesign:PackIcon Kind="TicketOutline" Width="80" Height="80" Foreground="{StaticResource TextMutedBrush}" HorizontalAlignment="Center"/>
                    <TextBlock Text="Chưa có mã khuyến mãi nào đang hoạt động!" FontSize="16" Foreground="{StaticResource TextMutedBrush}" HorizontalAlignment="Center" Margin="0,15,0,0"/>
                </StackPanel>

                <!-- Vouchers Grid -->
                <ItemsControl ItemsSource="{Binding Vouchers}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" HorizontalAlignment="Left"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <!-- Ticket Card -->
                            <Border Width="400" Height="140" Background="White" CornerRadius="8" Margin="0,0,20,20" Effect="{StaticResource CardShadow}" ClipToBounds="True">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Left: Highlight Area -->
                                    <Border Grid.Column="0" Background="{StaticResource BrandSecondaryBrush}">
                                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <materialDesign:PackIcon Kind="TicketPercent" Width="40" Height="40" Foreground="White" HorizontalAlignment="Center"/>
                                            <TextBlock Text="Mã Giảm Giá" Foreground="White" FontWeight="Bold" FontSize="14" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                                        </StackPanel>
                                    </Border>

                                    <!-- Right: Details Area -->
                                    <Grid Grid.Column="1" Margin="15">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <!-- Title & Shop -->
                                        <StackPanel Grid.Row="0">
                                            <TextBlock Text="{Binding VoucherName}" FontWeight="Bold" FontSize="16" Foreground="{StaticResource TextBrush}" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Text="{Binding Shop.ShopName, StringFormat='Áp dụng tại: {0}', FallbackValue='Áp dụng: Toàn Sàn'}" FontSize="12" Foreground="{StaticResource TextMutedBrush}" Margin="0,2,0,0" TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>

                                        <!-- Min Order Value & Discount -->
                                        <StackPanel Grid.Row="1" VerticalAlignment="Center">
                                            <TextBlock FontSize="13" Foreground="{StaticResource TextBrush}">
                                                <Run Text="Giảm "/>
                                                <Run Text="{Binding DiscountValue, StringFormat={}{0:N0}đ}" FontWeight="SemiBold" Foreground="{StaticResource PriceBrush}"/>
                                                <Run Text="{Binding DiscountType, StringFormat=' (Loại: {0})'}" Foreground="{StaticResource TextMutedBrush}"/>
                                            </TextBlock>
                                            <TextBlock Text="{Binding MinOrderValue, StringFormat='Đơn tối thiểu: {0:N0}đ'}" FontSize="12" Foreground="{StaticResource TextMutedBrush}" Margin="0,3,0,0"/>
                                        </StackPanel>

                                        <!-- Code & Action Button -->
                                        <Grid Grid.Row="2" Margin="0,5,0,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <!-- Code Box -->
                                            <Border Grid.Column="0" Background="#F1F5F9" CornerRadius="4" Padding="8,4" HorizontalAlignment="Left" BorderBrush="#E2E8F0" BorderThickness="1">
                                                <TextBlock Text="{Binding VoucherCode}" FontWeight="Bold" FontSize="14" Foreground="{StaticResource BrandPrimaryBrush}" HorizontalAlignment="Center"/>
                                            </Border>
                                            
                                            <!-- Copy Button -->
                                            <Button Grid.Column="1" Style="{StaticResource MaterialDesignFlatButton}" 
                                                    Content="Sao Chép" 
                                                    Command="{Binding DataContext.CopyVoucherCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" 
                                                    CommandParameter="{Binding VoucherCode}"
                                                    Foreground="{StaticResource BrandPrimaryBrush}" 
                                                    Padding="10,0"/>
                                        </Grid>
                                    </Grid>

                                    <!-- Dashed Divider -->
                                    <Line X1="120" Y1="0" X2="120" Y2="140" Stroke="#E0E0E0" StrokeThickness="2" StrokeDashArray="2 2"/>
                                    
                                    <!-- Cutout circles to make it look like a ticket -->
                                    <Ellipse Width="20" Height="20" Fill="{StaticResource BackgroundBrush}" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="110,-10,0,0"/>
                                    <Ellipse Width="20" Height="20" Fill="{StaticResource BackgroundBrush}" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="110,0,0,-10"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
