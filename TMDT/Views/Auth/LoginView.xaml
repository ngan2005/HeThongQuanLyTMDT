<Window x:Class="TMDT.Views.Auth.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:TMDT.ViewModels.Auth"
        mc:Ignorable="d"
        Title="LogisticsPro Manager - Đăng nhập" Height="750" Width="480"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        MouseDown="Window_MouseDown">

    <Window.DataContext>
        <vm:LoginViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <!-- Color Palette -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#C0C1FF"/>
        <SolidColorBrush x:Key="OnPrimaryBrush" Color="#1000A9"/>
        <SolidColorBrush x:Key="SecondaryBrush" Color="#B9C7DF"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#0B1326"/>
        <SolidColorBrush x:Key="OnSurfaceVariantBrush" Color="#C7C4D7"/>
        <SolidColorBrush x:Key="OutlineBrush" Color="#464554"/>

        <!-- Custom TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#0DFFFFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#20FFFFFF"/>
            <Setter Property="Padding" Value="45,15,15,15"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="1" CornerRadius="10">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                                <TextBlock x:Name="placeholder" Text="Nhập dữ liệu..." 
                                           Foreground="#40FFFFFF" Padding="45,0,0,0"
                                           VerticalAlignment="Center" IsHitTestVisible="False"
                                           Visibility="Collapsed"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="placeholder" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#C0C1FF"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Background Layer -->
        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="20">
             <Border.Effect>
                <DropShadowEffect BlurRadius="40" Color="Black" Opacity="0.5" ShadowDepth="0"/>
            </Border.Effect>
        </Border>
        
        <!-- Glow effects (Light Leaks) -->
        <Ellipse Fill="#C0C1FF" Opacity="0.1" Width="400" Height="400" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-200,-200,0,0">
            <Ellipse.Effect>
                <BlurEffect Radius="120"/>
            </Ellipse.Effect>
        </Ellipse>
        <Ellipse Fill="#BDC2FF" Opacity="0.1" Width="300" Height="300" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-150,-150">
            <Ellipse.Effect>
                <BlurEffect Radius="100"/>
            </Ellipse.Effect>
        </Ellipse>

        <!-- Main Card -->
        <Border Margin="1,1,48,10" CornerRadius="20" BorderBrush="#20FFFFFF" BorderThickness="1" Background="#10FFFFFF">
            <StackPanel Margin="45,60">
                
                <!-- Logo & Title -->
                <StackPanel HorizontalAlignment="Center" Margin="0,0,0,40">
                    <Border Width="70" Height="70" Background="{StaticResource PrimaryBrush}" CornerRadius="15" Margin="0,0,0,20">
                        <Border.Effect>
                            <DropShadowEffect Color="#C0C1FF" BlurRadius="20" ShadowDepth="0" Opacity="0.4"/>
                        </Border.Effect>
                        <TextBlock Text="" FontFamily="Segoe MDL2 Assets" FontSize="36" Foreground="{StaticResource OnPrimaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="Đăng nhập Hệ thống" FontSize="32" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                    <TextBlock Text="Quản lý Logistics chuyên nghiệp" FontSize="14" Foreground="{StaticResource OnSurfaceVariantBrush}" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>

                <!-- Input Fields -->
                <StackPanel>
                    <TextBlock Text="Tên đăng nhập" Foreground="{StaticResource OnSurfaceVariantBrush}" FontSize="12" FontWeight="SemiBold" Margin="5,0,0,8"/>
                    <Grid Margin="0,0,0,20">
                        <TextBox Style="{StaticResource ModernTextBox}" x:Name="txtUsername"/>
                        <TextBlock Text="" FontFamily="Segoe MDL2 Assets" FontSize="18" Foreground="{StaticResource OnSurfaceVariantBrush}" VerticalAlignment="Center" Margin="15,0,0,0"/>
                    </Grid>

                    <TextBlock Text="Mật khẩu" Foreground="{StaticResource OnSurfaceVariantBrush}" FontSize="12" FontWeight="SemiBold" Margin="5,0,0,8"/>
                    <Grid Margin="0,0,0,20">
                        <PasswordBox x:Name="txtPassword" Background="#0DFFFFFF" BorderBrush="#20FFFFFF" BorderThickness="1" Foreground="White" Padding="45,15,45,15"/>
                        <TextBlock Text="" FontFamily="Segoe MDL2 Assets" FontSize="18" Foreground="{StaticResource OnSurfaceVariantBrush}" VerticalAlignment="Center" Margin="15,0,0,0"/>
                        <Button HorizontalAlignment="Right" Background="Transparent" BorderThickness="0" Margin="0,0,15,0" Cursor="Hand">
                            <TextBlock Text="" FontFamily="Segoe MDL2 Assets" Foreground="{StaticResource OnSurfaceVariantBrush}"/>
                        </Button>
                    </Grid>

                    <TextBlock Text="Vai trò" Foreground="{StaticResource OnSurfaceVariantBrush}" FontSize="12" FontWeight="SemiBold" Margin="5,0,0,8"/>
                    <ComboBox Height="50" Background="#0D976161" Foreground="White" BorderBrush="#20FFFFFF" VerticalContentAlignment="Center" Padding="10,0" IsDropDownOpen="True">
                        <ComboBoxItem Content="Admin" IsSelected="True"/>
                        <ComboBoxItem Content="Nhân viên"/>
                    </ComboBox>

                    <DockPanel Margin="0,20,0,30">
                        <CheckBox Content="Ghi nhớ mật khẩu" Foreground="{StaticResource OnSurfaceVariantBrush}" VerticalContentAlignment="Center"/>
                        <TextBlock Text="Quên mật khẩu?" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Right" Cursor="Hand" VerticalAlignment="Center"/>
                    </DockPanel>

                    <Button Content="Đăng nhập hệ thống" Command="{Binding LoginCommand}" Height="55" Background="{StaticResource PrimaryBrush}" Foreground="{StaticResource OnPrimaryBrush}" FontWeight="Bold" FontSize="18" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="12">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Resources>
                        <Button.Effect>
                            <DropShadowEffect Color="#C0C1FF" BlurRadius="25" ShadowDepth="0" Opacity="0.3"/>
                        </Button.Effect>
                    </Button>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                        <TextBlock Text="Chưa có tài khoản?" Foreground="{StaticResource OnSurfaceVariantBrush}"/>
                        <Button Content="Đăng ký tại đây" Click="GoToRegister_Click" Background="Transparent" Foreground="{StaticResource PrimaryBrush}" BorderThickness="0" Cursor="Hand" Margin="5,0,0,0"/>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>
