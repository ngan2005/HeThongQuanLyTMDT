<UserControl x:Class="TMDT.Views.Seller.SellerProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TMDT.Views.Seller"
             xmlns:converters="clr-namespace:TMDT.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="580" d:DesignWidth="820">

    <UserControl.Resources>
        <!-- Converter for checking active status filter -->
        <converters:EqualityToBooleanConverter x:Key="EqualityToBooleanConverter"/>

        <!-- Premium Button Styles -->
        <Style x:Key="SearchBox" TargetType="TextBox">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#0D9488"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FilterBtn" TargetType="RadioButton">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="GroupName" Value="ProductFilter"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F8FAFC"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0D9488"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#0D9488"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InputLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11.5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
            <Setter Property="FontFamily" Value="Be Vietnam Pro, Segoe UI"/>
        </Style>

        <Style x:Key="FormInput" TargetType="TextBox">
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFFFFF"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#0D9488"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <!-- ========================================== -->
        <!-- MAIN CONTENT: FULL WIDTH PRODUCTS LIST -->
        <!-- ========================================== -->
        <Border Background="#FFFFFF" CornerRadius="16" BorderThickness="1" BorderBrush="#F1F5F9" Padding="24" Margin="0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="16" ShadowDepth="2" Opacity="0.04" Color="#000000" Direction="270"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <!-- Search & Filter Header -->
                    <RowDefinition Height="Auto"/>
                    <!-- DataGrid -->
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Title, Add New Button, and Search Box -->
                    <Grid Grid.Row="0" Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Grid.Column="0" Text="Danh Sách Sản Phẩm" FontSize="18" FontWeight="Bold" Foreground="#1E293B" VerticalAlignment="Center" FontFamily="Be Vietnam Pro, Segoe UI"/>
                        
                        <Button Grid.Column="1" Click="AddNewProduct_Click" Margin="20,0,0,0" Background="Transparent" BorderThickness="0" Cursor="Hand" ToolTip="Đăng bán sản phẩm mới" VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE109;" FontFamily="Segoe MDL2 Assets" FontSize="12" Foreground="#0D9488" FontWeight="Bold" VerticalAlignment="Center"/>
                                <TextBlock Text=" Đăng bán sản phẩm mới" FontSize="13" Foreground="#0D9488" FontWeight="Bold" VerticalAlignment="Center" FontFamily="Be Vietnam Pro, Segoe UI"/>
                            </StackPanel>
                        </Button>
                        
                        <TextBox Grid.Column="2" Style="{StaticResource SearchBox}" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="220" HorizontalAlignment="Right" VerticalAlignment="Center" ToolTip="Tìm theo Tên hoặc Mã sản phẩm"/>
                        <TextBlock Grid.Column="2" IsHitTestVisible="False" Text="&#xE721; Tìm sản phẩm..." FontFamily="Segoe MDL2 Assets" FontSize="11.5" Foreground="#94A3B8" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,12,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SearchText}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>

                    <!-- Filters bar -->
                    <StackPanel Grid.Row="1" Orientation="Horizontal">
                        <RadioButton Style="{StaticResource FilterBtn}" Content="Tất cả" Command="{Binding SetFilterCommand}" CommandParameter="All" IsChecked="{Binding StatusFilter, Converter={StaticResource EqualityToBooleanConverter}, ConverterParameter=All, Mode=OneWay}" Margin="0,0,8,0"/>
                        <RadioButton Style="{StaticResource FilterBtn}" Content="Đang bán" Command="{Binding SetFilterCommand}" CommandParameter="Approved" IsChecked="{Binding StatusFilter, Converter={StaticResource EqualityToBooleanConverter}, ConverterParameter=Approved, Mode=OneWay}" Margin="0,0,8,0"/>
                        <RadioButton Style="{StaticResource FilterBtn}" Content="Chờ duyệt" Command="{Binding SetFilterCommand}" CommandParameter="Pending" IsChecked="{Binding StatusFilter, Converter={StaticResource EqualityToBooleanConverter}, ConverterParameter=Pending, Mode=OneWay}" Margin="0,0,8,0"/>
                        <RadioButton Style="{StaticResource FilterBtn}" Content="Từ chối" Command="{Binding SetFilterCommand}" CommandParameter="Rejected" IsChecked="{Binding StatusFilter, Converter={StaticResource EqualityToBooleanConverter}, ConverterParameter=Rejected, Mode=OneWay}" Margin="0,0,8,0"/>
                        <RadioButton Style="{StaticResource FilterBtn}" Content="Đã xóa" Command="{Binding SetFilterCommand}" CommandParameter="Deleted" IsChecked="{Binding StatusFilter, Converter={StaticResource EqualityToBooleanConverter}, ConverterParameter=Deleted, Mode=OneWay}"/>
                    </StackPanel>
                </Grid>

                <!-- DataGrid List of Products -->
                <DataGrid Grid.Row="1" ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct, Mode=TwoWay}"
                          AutoGenerateColumns="False" HeadersVisibility="Column" GridLinesVisibility="None" Background="Transparent" 
                          BorderThickness="0" RowHeight="48" IsReadOnly="True" MouseDoubleClick="DataGrid_MouseDoubleClick">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F8FAFC"/>
                            <Setter Property="Foreground" Value="#64748B"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontSize" Value="11"/>
                            <Setter Property="Padding" Value="12,6"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#E2E8F0"/>
                            <Setter Property="FontFamily" Value="Be Vietnam Pro, Segoe UI"/>
                        </Style>
                        <Style TargetType="DataGridRow">
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#F1F5F9"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#F0FDFA"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F8FAFC"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="12,0"/>
                            <Setter Property="FontSize" Value="12.5"/>
                            <Setter Property="Foreground" Value="#1E293B"/>
                            <Setter Property="FontFamily" Value="Be Vietnam Pro, Segoe UI"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Foreground" Value="#0D9488"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã Code" Binding="{Binding ProductCode}" Width="110">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="#64748B"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        
                        <DataGridTextColumn Header="Tên Sản Phẩm" Binding="{Binding ProductName}" Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        
                        <DataGridTextColumn Header="Giá Bán" Binding="{Binding Price, StringFormat={}{0:N0} đ}" Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="#0D9488"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        
                        <DataGridTextColumn Header="Tồn Kho" Binding="{Binding StockQuantity}" Width="90">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Medium"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        
                        <DataGridTemplateColumn Header="Trạng Thái" Width="110">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="6" Padding="8,3" HorizontalAlignment="Left" VerticalAlignment="Center">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="Pending">
                                                        <Setter Property="Background" Value="#FFEFE2"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Approved">
                                                        <Setter Property="Background" Value="#ECFDF5"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Rejected">
                                                        <Setter Property="Background" Value="#FEE2E2"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock FontSize="10.5" FontWeight="Bold" FontFamily="Be Vietnam Pro, Segoe UI" VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="Pending">
                                                            <Setter Property="Text" Value="Chờ duyệt"/>
                                                            <Setter Property="Foreground" Value="#EA580C"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Approved">
                                                            <Setter Property="Text" Value="Đang bán"/>
                                                            <Setter Property="Foreground" Value="#059669"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Rejected">
                                                            <Setter Property="Text" Value="Từ chối"/>
                                                            <Setter Property="Foreground" Value="#EF4444"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="Thao Tác" Width="90">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Click="OpenDetail_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" Padding="4" ToolTip="Xem chi tiết &amp; Chỉnh sửa" HorizontalAlignment="Center">
                                        <Border Width="28" Height="28" CornerRadius="6" Background="#F1F5F9">
                                            <TextBlock Text="&#xE70F;" FontFamily="Segoe MDL2 Assets" FontSize="11" Foreground="#64748B" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- ========================================== -->
        <!-- LIGHTBOX OVERLAY: PRODUCT DETAIL DIALOG -->
        <!-- ========================================== -->
        <Grid x:Name="LightboxOverlay" Visibility="Collapsed" Background="#990F172A" Grid.ColumnSpan="1" MouseDown="LightboxOverlay_MouseDown">
            <Border Background="#FFFFFF" CornerRadius="16" BorderThickness="1" BorderBrush="#E2E8F0"
                    Width="600" MaxHeight="540" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Border.Effect>
                    <DropShadowEffect Color="#0D9488" BlurRadius="30" ShadowDepth="0" Opacity="0.12"/>
                </Border.Effect>

                <Grid>
                    <Grid.RowDefinitions>
                        <!-- Header -->
                        <RowDefinition Height="Auto"/>
                        <!-- Fields Form -->
                        <RowDefinition Height="*"/>
                        <!-- Footer Actions -->
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Header Grid -->
                    <Border Grid.Row="0" CornerRadius="16,16,0,0" Padding="24,18,24,18" BorderThickness="0,0,0,1" BorderBrush="#F1F5F9">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#F8FAFC" Offset="0"/>
                                <GradientStop Color="#FFFFFF" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Width="36" Height="36" Background="#F0FDFA" CornerRadius="8" Margin="0,0,12,0">
                                <TextBlock Text="&#xE71B;" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="#0D9488" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding FormTitle}" FontSize="15" FontWeight="Bold" Foreground="#1E293B" FontFamily="Be Vietnam Pro, Segoe UI"/>
                                <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                    <Border CornerRadius="4" Padding="6,2" VerticalAlignment="Center">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#F0FDFA"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding SelectedProduct.Status}" Value="Pending">
                                                        <Setter Property="Background" Value="#FFF7ED"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding SelectedProduct.Status}" Value="Approved">
                                                        <Setter Property="Background" Value="#ECFDF5"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding SelectedProduct.Status}" Value="Rejected">
                                                        <Setter Property="Background" Value="#FEF2F2"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding SelectedProduct.Status}" Value="Deleted">
                                                        <Setter Property="Background" Value="#F1F5F9"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock FontSize="9.5" FontWeight="Bold" FontFamily="Be Vietnam Pro, Segoe UI" VerticalAlignment="Center" Text="{Binding FormStatusBadge}"/>
                                    </Border>
                                </StackPanel>
                            </StackPanel>

                            <!-- Close Button (top right) -->
                            <Button Grid.Column="2" Click="CloseLightbox" Background="Transparent" BorderThickness="0" Width="32" Height="32" Cursor="Hand" VerticalAlignment="Center">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="bd" Background="Transparent" CornerRadius="6">
                                            <TextBlock Text="&#xE106;" FontFamily="Segoe MDL2 Assets" FontSize="12" Foreground="#64748B" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="bd" Property="Background" Value="#F1F5F9"/>
                                                <Setter Property="Foreground" Value="#1E293B"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </Grid>
                    </Border>

                    <!-- Form Inputs -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="24,20,24,10">
                        <StackPanel>
                            <!-- Product Code -->
                            <TextBlock Style="{StaticResource InputLabel}" Text="MÃ SẢN PHẨM (MÃ SKU)"/>
                            <Border BorderThickness="1" BorderBrush="#E2E8F0" CornerRadius="6" Margin="0,0,0,12" Background="#F8FAFC">
                                <TextBox Style="{StaticResource FormInput}" Text="{Binding ProductCodeInput, UpdateSourceTrigger=PropertyChanged}" BorderThickness="0" Height="36" VerticalContentAlignment="Center"/>
                            </Border>

                            <!-- Product Name -->
                            <TextBlock Style="{StaticResource InputLabel}" Text="TÊN SẢN PHẨM *"/>
                            <Border BorderThickness="1" BorderBrush="#E2E8F0" CornerRadius="6" Margin="0,0,0,12" Background="#F8FAFC">
                                <TextBox Style="{StaticResource FormInput}" Text="{Binding ProductNameInput, UpdateSourceTrigger=PropertyChanged}" BorderThickness="0" Height="36" VerticalContentAlignment="Center"/>
                            </Border>

                            <!-- Category ComboBox -->
                            <TextBlock Style="{StaticResource InputLabel}" Text="DANH MỤC NGÀNH HÀNG"/>
                            <Border BorderThickness="1" BorderBrush="#E2E8F0" CornerRadius="6" Margin="0,0,0,12" Background="#F8FAFC">
                                <ComboBox ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategoryInput}" DisplayMemberPath="CategoryName" Height="36" BorderThickness="0" Padding="8,0">
                                    <ComboBox.Resources>
                                        <Style TargetType="ComboBox">
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                                        </Style>
                                    </ComboBox.Resources>
                                </ComboBox>
                            </Border>

                            <!-- Row for Price & Original Price -->
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="12"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- Price -->
                                <StackPanel Grid.Column="0">
                                    <TextBlock Style="{StaticResource InputLabel}" Text="GIÁ BÁN (đ) *"/>
                                    <Border BorderThickness="1" BorderBrush="#E2E8F0" CornerRadius="6" Background="#F8FAFC">
                                        <TextBox Style="{StaticResource FormInput}" Text="{Binding PriceInput, UpdateSourceTrigger=PropertyChanged}" BorderThickness="0" Height="36" VerticalContentAlignment="Center"/>
                                    </Border>
                                </StackPanel>
                                
                                <!-- Original Price -->
                                <StackPanel Grid.Column="2">
                                    <TextBlock Style="{StaticResource InputLabel}" Text="GIÁ GỐC (đ)"/>
                                    <Border BorderThickness="1" BorderBrush="#E2E8F0" CornerRadius="6" Background="#F8FAFC">
                                        <TextBox Style="{StaticResource FormInput}" Text="{Binding OriginalPriceInput, UpdateSourceTrigger=PropertyChanged}" BorderThickness="0" Height="36" VerticalContentAlignment="Center"/>
                                    </Border>
                                </StackPanel>
                            </Grid>

                            <!-- Stock -->
                            <TextBlock Style="{StaticResource InputLabel}" Text="SỐ LƯỢNG TỒN KHO"/>
                            <Border BorderThickness="1" BorderBrush="#E2E8F0" CornerRadius="6" Margin="0,0,0,12" Background="#F8FAFC">
                                <TextBox Style="{StaticResource FormInput}" Text="{Binding StockInput, UpdateSourceTrigger=PropertyChanged}" BorderThickness="0" Height="36" VerticalContentAlignment="Center"/>
                            </Border>

                            <!-- Description -->
                            <TextBlock Style="{StaticResource InputLabel}" Text="MÔ TẢ SẢN PHẨM"/>
                            <Border BorderThickness="1" BorderBrush="#E2E8F0" CornerRadius="6" Background="#F8FAFC" Height="80">
                                <TextBox Style="{StaticResource FormInput}" Text="{Binding DescriptionInput, UpdateSourceTrigger=PropertyChanged}" BorderThickness="0" Height="78" TextWrapping="Wrap" AcceptsReturn="True" VerticalContentAlignment="Top"/>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- Action buttons -->
                    <Border Grid.Row="2" Padding="24,14,24,18" BorderThickness="0,1,0,0" BorderBrush="#F1F5F9">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Delete button — chỉ hiện khi đang sửa sản phẩm chưa bị xóa -->
                            <Button Grid.Column="0" Command="{Binding DeleteProductCommand}" Click="CloseLightbox" Background="#FEE2E2" BorderThickness="0" Height="38" Width="38" Cursor="Hand" ToolTip="Xóa sản phẩm" Margin="0,0,8,0">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                            <TextBlock Text="&#xE74D;" FontFamily="Segoe MDL2 Assets" FontSize="12" Foreground="#EF4444" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsEditMode}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>

                            <!-- Save / Add button -->
                            <Button Grid.Column="1" Command="{Binding SaveProductCommand}" Click="CloseLightbox" Height="38" Cursor="Hand">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                                <Button.Content>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="11.5" Foreground="#FFFFFF" VerticalAlignment="Center" Margin="0,0,8,0">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="&#xE105;"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsEditMode}" Value="True">
                                                            <Setter Property="Text" Value="&#xE74E;"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <TextBlock VerticalAlignment="Center" FontSize="13" Foreground="#FFFFFF" FontWeight="Bold" FontFamily="Be Vietnam Pro, Segoe UI">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value=" Đăng bán sản phẩm"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsEditMode}" Value="True">
                                                            <Setter Property="Text" Value=" Lưu thay đổi"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </Button.Content>
                                <Button.Background>
                                    <SolidColorBrush Color="#0D9488"/>
                                </Button.Background>
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Opacity" Value="0.9"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Grid>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
