<Window x:Class="TMDT.Views.Seller.ReceiptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Hóa đơn thanh toán" Height="620" Width="430"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">
    <Border Background="#F1F5F9" CornerRadius="14" Margin="16" Effect="{StaticResource CardShadow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Custom Header -->
            <Border Grid.Row="0" Background="#0F766E" CornerRadius="14,14,0,0" Padding="16,12" MouseLeftButtonDown="Header_MouseDown" Cursor="SizeAll">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <materialDesign:PackIcon Kind="ReceiptText" Width="22" Height="22" Foreground="White" Margin="0,0,8,0"/>
                        <TextBlock Text="In Hóa Đơn" FontSize="16" FontWeight="Bold" Foreground="White"/>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" VerticalAlignment="Center" 
                            Click="Close_Click" Padding="0" Width="28" Height="28"
                            Style="{StaticResource MaterialDesignFlatButton}" Foreground="#99F6E4" ToolTip="Đóng">
                        <materialDesign:PackIcon Kind="Close" Width="18" Height="18"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Receipt Content (This will be printed) -->
            <ScrollViewer Grid.Row="1" Margin="16" Background="White" Effect="{StaticResource CardShadow}">
            <!-- The 300px width is roughly 80mm thermal paper -->
            <Border x:Name="ReceiptPrintArea" Width="320" HorizontalAlignment="Center" Padding="16" Background="White">
                <StackPanel>
                    <!-- Header -->
                    <TextBlock Text="HỆ THỐNG TMĐT" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="--- HÓA ĐƠN BÁN LẺ ---" FontSize="13" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                    
                    <Line X1="0" Y1="0" X2="300" Y2="0" Stroke="Black" StrokeDashArray="2,2" StrokeThickness="1" Margin="0,12"/>
                    
                    <TextBlock FontSize="11" Margin="0,2">
                        <Run Text="Số phiếu: "/>
                        <Run Text="{Binding OrderCode}" FontWeight="SemiBold"/>
                    </TextBlock>
                    <TextBlock FontSize="11" Margin="0,2">
                        <Run Text="Ngày: "/>
                        <Run Text="{Binding OrderDate}"/>
                    </TextBlock>
                    <TextBlock FontSize="11" Margin="0,2">
                        <Run Text="Thu ngân: "/>
                        <Run Text="{Binding StaffName}" FontWeight="SemiBold"/>
                    </TextBlock>
                    
                    <Line X1="0" Y1="0" X2="300" Y2="0" Stroke="Black" StrokeDashArray="2,2" StrokeThickness="1" Margin="0,12"/>
                    
                    <!-- Items -->
                    <Grid Margin="0,0,0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="40"/>
                            <ColumnDefinition Width="70"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Sản phẩm" FontSize="11" FontWeight="Bold"/>
                        <TextBlock Grid.Column="1" Text="SL" FontSize="11" FontWeight="Bold" TextAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="T.Tiền" FontSize="11" FontWeight="Bold" TextAlignment="Right"/>
                    </Grid>
                    
                    <ItemsControl ItemsSource="{Binding OrderDetails}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="40"/>
                                        <ColumnDefinition Width="70"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding ProductNameSnapshot}" FontSize="11" TextWrapping="Wrap"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Quantity}" FontSize="11" TextAlignment="Center" VerticalAlignment="Top"/>
                                    <TextBlock Grid.Column="2" Text="{Binding TotalPrice, StringFormat={}{0:N0}}" FontSize="11" TextAlignment="Right" VerticalAlignment="Top"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    
                    <Line X1="0" Y1="0" X2="300" Y2="0" Stroke="Black" StrokeDashArray="2,2" StrokeThickness="1" Margin="0,12"/>
                    
                    <!-- Totals -->
                    <Grid Margin="0,2">
                        <TextBlock Text="Tổng tiền hàng:" FontSize="12"/>
                        <TextBlock Text="{Binding SubTotal, StringFormat={}{0:N0}}" FontSize="12" HorizontalAlignment="Right"/>
                    </Grid>
                    <Grid Margin="0,2" Visibility="{Binding DiscountVisibility}">
                        <TextBlock Text="Giảm giá/Điểm:" FontSize="12"/>
                        <TextBlock Text="{Binding Discount, StringFormat=-{0:N0}}" FontSize="12" HorizontalAlignment="Right"/>
                    </Grid>
                    
                    <Grid Margin="0,8,0,0">
                        <TextBlock Text="TỔNG THANH TOÁN:" FontSize="14" FontWeight="Bold"/>
                        <TextBlock Text="{Binding TotalAmount, StringFormat={}{0:N0}}" FontSize="14" FontWeight="Bold" HorizontalAlignment="Right"/>
                    </Grid>
                    
                    <!-- Payment Info -->
                    <StackPanel Margin="0,12,0,0">
                        <Grid Margin="0,2">
                            <TextBlock Text="Hình thức TT:" FontSize="12"/>
                            <TextBlock Text="{Binding PaymentMethod}" FontSize="12" HorizontalAlignment="Right"/>
                        </Grid>
                        <Grid Margin="0,2" Visibility="{Binding CashVisibility}">
                            <TextBlock Text="Tiền khách đưa:" FontSize="12"/>
                            <TextBlock Text="{Binding GivenAmount, StringFormat={}{0:N0}}" FontSize="12" HorizontalAlignment="Right"/>
                        </Grid>
                        <Grid Margin="0,2" Visibility="{Binding CashVisibility}">
                            <TextBlock Text="Tiền thừa:" FontSize="12"/>
                            <TextBlock Text="{Binding ChangeAmount, StringFormat={}{0:N0}}" FontSize="12" HorizontalAlignment="Right"/>
                        </Grid>
                    </StackPanel>
                    
                    <!-- Điểm tích lũy -->
                    <Grid Margin="0,2" Visibility="{Binding EarnedPointsVisibility}">
                        <TextBlock Text="Điểm tích lũy được:" FontSize="12" Foreground="#0D9488"/>
                        <TextBlock Text="{Binding EarnedPoints, StringFormat=+{0} điểm}" FontSize="12" FontWeight="Bold" Foreground="#0D9488" HorizontalAlignment="Right"/>
                    </Grid>
                    
                    <Line X1="0" Y1="0" X2="300" Y2="0" Stroke="Black" StrokeDashArray="2,2" StrokeThickness="1" Margin="0,12"/>
                    
                    <TextBlock Text="Cảm ơn quý khách và hẹn gặp lại!" FontSize="12" HorizontalAlignment="Center" FontStyle="Italic" Margin="0,4,0,0"/>
                    
                    <!-- Barcode Mockup (just a font representation) -->
                    <TextBlock Text="*123456789*" FontFamily="Code39, Consolas" FontSize="24" HorizontalAlignment="Center" Margin="0,12,0,0"/>
                </StackPanel>
            </Border>
        </ScrollViewer>

            <!-- Actions -->
            <Border Grid.Row="2" Background="White" Padding="16" BorderThickness="0,1,0,0" BorderBrush="#E2E8F0" CornerRadius="0,0,14,14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="In Hóa đơn (Print)" Click="Print_Click" Margin="0,0,8,0" Background="#0D9488" BorderBrush="#0D9488"/>
                    <Button Grid.Column="1" Content="Xuất PDF" Click="ExportPdf_Click" Margin="0,0,16,0" Background="#2563EB" BorderBrush="#2563EB"/>
                    <Button Grid.Column="2" Content="Đóng" Click="Close_Click" Style="{StaticResource MaterialDesignOutlinedButton}" Width="80"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
