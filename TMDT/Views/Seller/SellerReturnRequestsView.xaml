<UserControl x:Class="TMDT.Views.Seller.SellerReturnRequestsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TMDT.Views.Seller"
             xmlns:vm="clr-namespace:TMDT.ViewModels.Seller"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000" Background="#F1F5F9">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <Style x:Key="FilterButtonStyle" TargetType="RadioButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="16,8" Margin="0,0,12,0" Cursor="Hand">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F0FDFA"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#0D9488"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F8FAFC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="FontFamily" Value="Be Vietnam Pro, Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Foreground" Value="#0D9488"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ApproveButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#10B981"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontFamily" Value="Be Vietnam Pro, Segoe UI"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.9"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="RejectButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#EF4444"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontFamily" Value="Be Vietnam Pro, Segoe UI"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.9"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Trả hàng / Hoàn tiền" FontSize="24" FontWeight="Bold" Foreground="#1E293B" FontFamily="Be Vietnam Pro, Segoe UI" Margin="0,0,0,24"/>

        <!-- Filters -->
        <Border Grid.Row="1" Background="#FFFFFF" CornerRadius="12" Padding="24" Margin="0,0,0,24" BorderThickness="1" BorderBrush="#E2E8F0">
            <Border.Effect>
                <DropShadowEffect Color="#0F172A" BlurRadius="20" ShadowDepth="4" Opacity="0.03"/>
            </Border.Effect>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Trạng thái:" VerticalAlignment="Center" Foreground="#64748B" FontFamily="Be Vietnam Pro, Segoe UI" FontSize="14" Margin="0,0,16,0"/>
                <RadioButton Content="Tất cả" GroupName="StatusGrp" IsChecked="True" Style="{StaticResource FilterButtonStyle}" Command="{Binding SetFilterCommand}" CommandParameter="All"/>
                <RadioButton Content="Chờ xử lý" GroupName="StatusGrp" Style="{StaticResource FilterButtonStyle}" Command="{Binding SetFilterCommand}" CommandParameter="Pending"/>
                <RadioButton Content="Đã chấp nhận" GroupName="StatusGrp" Style="{StaticResource FilterButtonStyle}" Command="{Binding SetFilterCommand}" CommandParameter="Approved"/>
                <RadioButton Content="Đã từ chối" GroupName="StatusGrp" Style="{StaticResource FilterButtonStyle}" Command="{Binding SetFilterCommand}" CommandParameter="Rejected"/>
            </StackPanel>
        </Border>

        <!-- Requests List -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Requests}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <!-- Request Card -->
                        <Border Background="#FFFFFF" CornerRadius="12" Padding="20" Margin="0,0,0,16" BorderThickness="1" BorderBrush="#E2E8F0">
                            <Border.Effect>
                                <DropShadowEffect Color="#0F172A" BlurRadius="20" ShadowDepth="4" Opacity="0.03"/>
                            </Border.Effect>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="300"/>
                                </Grid.ColumnDefinitions>

                                <!-- Info Area -->
                                <StackPanel Grid.Column="0" Margin="0,0,24,0">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                        <Border Background="#F1F5F9" CornerRadius="4" Padding="8,4" Margin="0,0,12,0">
                                            <TextBlock Text="{Binding OrderCode, StringFormat={}Mã ĐH: {0}}" FontWeight="Bold" FontSize="13" Foreground="#475569" FontFamily="Be Vietnam Pro, Segoe UI"/>
                                        </Border>
                                        <TextBlock Text="{Binding BuyerName}" FontWeight="Bold" FontSize="15" Foreground="#1E293B" FontFamily="Be Vietnam Pro, Segoe UI" VerticalAlignment="Center"/>
                                        <TextBlock Text=" • " Foreground="#94A3B8" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding RequestDate}" FontSize="13" Foreground="#64748B" FontFamily="Be Vietnam Pro, Segoe UI" VerticalAlignment="Center"/>
                                    </StackPanel>

                                    <!-- Product Details -->
                                    <Grid Margin="0,0,0,16">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="100"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="Sản phẩm:" Foreground="#64748B" FontFamily="Be Vietnam Pro, Segoe UI" FontSize="14"/>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="{Binding ProductName}" FontWeight="SemiBold" Foreground="#1E293B" FontFamily="Be Vietnam Pro, Segoe UI" FontSize="14" TextWrapping="Wrap"/>
                                            <TextBlock Text="{Binding Quantity, StringFormat={}Số lượng: x{0}}" Foreground="#64748B" FontFamily="Be Vietnam Pro, Segoe UI" FontSize="13" Margin="0,4,0,0"/>
                                            <TextBlock Text="{Binding TotalPrice, StringFormat={}{0:N0} đ}" FontWeight="Bold" Foreground="#EF4444" FontFamily="Be Vietnam Pro, Segoe UI" FontSize="14" Margin="0,4,0,0"/>
                                        </StackPanel>
                                    </Grid>

                                    <!-- Reason Details -->
                                    <Grid Margin="0,0,0,16">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="100"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="Lý do trả:" Foreground="#64748B" FontFamily="Be Vietnam Pro, Segoe UI" FontSize="14"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Reason}" Foreground="#334155" FontFamily="Be Vietnam Pro, Segoe UI" FontSize="14" TextWrapping="Wrap"/>
                                    </Grid>

                                    <!-- Evidence -->
                                    <Grid Visibility="{Binding HasEvidence, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="100"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="Bằng chứng:" Foreground="#64748B" FontFamily="Be Vietnam Pro, Segoe UI" FontSize="14"/>
                                        <Border Grid.Column="1" Width="120" Height="120" CornerRadius="8" HorizontalAlignment="Left" ClipToBounds="True">
                                            <Image Source="{Binding EvidenceImage}" Stretch="UniformToFill"/>
                                        </Border>
                                    </Grid>
                                </StackPanel>

                                <!-- Action Area -->
                                <Border Grid.Column="1" Background="#F8FAFC" CornerRadius="8" Padding="20" BorderThickness="1" BorderBrush="#E2E8F0">
                                    <StackPanel VerticalAlignment="Center">
                                        
                                        <!-- Status Display -->
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,16">
                                            <TextBlock Text="Trạng thái: " Foreground="#64748B" FontFamily="Be Vietnam Pro, Segoe UI" FontSize="14" VerticalAlignment="Center"/>
                                            <Border CornerRadius="4" Padding="8,4">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="#FEF3C7"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Status}" Value="Approved">
                                                                <Setter Property="Background" Value="#D1FAE5"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Rejected">
                                                                <Setter Property="Background" Value="#FEE2E2"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="{Binding StatusText}" FontWeight="Bold" FontSize="13" FontFamily="Be Vietnam Pro, Segoe UI">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="#D97706"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Status}" Value="Approved">
                                                                    <Setter Property="Foreground" Value="#059669"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Rejected">
                                                                    <Setter Property="Foreground" Value="#DC2626"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Border>
                                        </StackPanel>

                                        <!-- Processed Time (if not pending) -->
                                        <TextBlock Text="{Binding ProcessedDate, StringFormat={}Xử lý lúc: {0}}" 
                                                   FontSize="12" Foreground="#94A3B8" FontFamily="Be Vietnam Pro, Segoe UI" 
                                                   HorizontalAlignment="Center" Margin="0,0,0,16"
                                                   Visibility="{Binding IsProcessed, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                                        <!-- Action Buttons (Only when Pending) -->
                                        <StackPanel Visibility="{Binding IsPending, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <Button Content="Chấp nhận trả hàng" 
                                                    Command="{Binding DataContext.ApproveRequestCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                    CommandParameter="{Binding}"
                                                    Style="{StaticResource ApproveButtonStyle}" Margin="0,0,0,12"/>
                                            
                                            <Button Content="Từ chối yêu cầu" 
                                                    Command="{Binding DataContext.RejectRequestCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                    CommandParameter="{Binding}"
                                                    Style="{StaticResource RejectButtonStyle}"/>
                                        </StackPanel>

                                        <TextBlock Text="Đã xử lý xong" FontSize="14" FontWeight="SemiBold" Foreground="#10B981" FontFamily="Be Vietnam Pro, Segoe UI" HorizontalAlignment="Center"
                                                   Visibility="{Binding IsProcessed, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="Rejected">
                                                            <Setter Property="Foreground" Value="#EF4444"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
