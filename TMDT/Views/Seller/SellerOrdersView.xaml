<UserControl x:Class="TMDT.Views.Seller.SellerOrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TMDT.Views.Seller"
             xmlns:converters="clr-namespace:TMDT.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="580" d:DesignWidth="820">

    <UserControl.Resources>
        <!-- Converter for checking active status filter -->
        <converters:EqualityToBooleanConverter x:Key="EqualityToBooleanConverter"/>

        <DropShadowEffect x:Key="CardShadow" BlurRadius="16" ShadowDepth="2" Opacity="0.06" Color="#000" Direction="270"/>

        <Style x:Key="TabBtn" TargetType="RadioButton">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="GroupName" Value="OrderTabFilter"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F8FAFC"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0D9488"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#0D9488"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LabelHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11.5"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
            <Setter Property="FontFamily" Value="Be Vietnam Pro, Segoe UI"/>
        </Style>

        <Style x:Key="ValueText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12.5"/>
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="FontFamily" Value="Be Vietnam Pro, Segoe UI"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <!-- ========================================== -->
        <!-- MAIN CONTENT: FULL-WIDTH ORDERS WORKSPACE -->
        <!-- ========================================== -->
        <Border Background="#FFFFFF" CornerRadius="16" BorderThickness="1" BorderBrush="#F1F5F9" Padding="24">
            <Border.Effect>
                <StaticResource ResourceKey="CardShadow"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <!-- Header -->
                    <RowDefinition Height="Auto"/>
                    <!-- List -->
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header & Status Tabs -->
                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Quản Lý Đơn Đặt Hàng" FontSize="16" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,14" FontFamily="Be Vietnam Pro, Segoe UI"/>
                    
                    <StackPanel Grid.Row="1" Orientation="Horizontal">
                        <RadioButton Style="{StaticResource TabBtn}" Content="Tất cả" Command="{Binding SetFilterCommand}" CommandParameter="All" IsChecked="{Binding StatusFilter, Converter={StaticResource EqualityToBooleanConverter}, ConverterParameter=All, Mode=OneWay}" Margin="0,0,8,0"/>
                        <RadioButton Style="{StaticResource TabBtn}" Content="Chờ duyệt" Command="{Binding SetFilterCommand}" CommandParameter="Pending" IsChecked="{Binding StatusFilter, Converter={StaticResource EqualityToBooleanConverter}, ConverterParameter=Pending, Mode=OneWay}" Margin="0,0,8,0"/>
                        <RadioButton Style="{StaticResource TabBtn}" Content="Đang giao" Command="{Binding SetFilterCommand}" CommandParameter="Shipping" IsChecked="{Binding StatusFilter, Converter={StaticResource EqualityToBooleanConverter}, ConverterParameter=Shipping, Mode=OneWay}" Margin="0,0,8,0"/>
                        <RadioButton Style="{StaticResource TabBtn}" Content="Đã xong" Command="{Binding SetFilterCommand}" CommandParameter="Completed" IsChecked="{Binding StatusFilter, Converter={StaticResource EqualityToBooleanConverter}, ConverterParameter=Completed, Mode=OneWay}" Margin="0,0,8,0"/>
                        <RadioButton Style="{StaticResource TabBtn}" Content="Đã hủy" Command="{Binding SetFilterCommand}" CommandParameter="Cancelled" IsChecked="{Binding StatusFilter, Converter={StaticResource EqualityToBooleanConverter}, ConverterParameter=Cancelled, Mode=OneWay}"/>
                    </StackPanel>
                </Grid>

                <!-- DataGrid Orders List -->
                <DataGrid Grid.Row="1" ItemsSource="{Binding Orders}" SelectedItem="{Binding SelectedOrder, Mode=TwoWay}"
                          AutoGenerateColumns="False" HeadersVisibility="Column" GridLinesVisibility="None" Background="Transparent" 
                          BorderThickness="0" RowHeight="44" IsReadOnly="True" MouseDoubleClick="DataGrid_MouseDoubleClick">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F8FAFC"/>
                            <Setter Property="Foreground" Value="#64748B"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontSize" Value="11"/>
                            <Setter Property="Padding" Value="12,4"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#E2E8F0"/>
                            <Setter Property="FontFamily" Value="Be Vietnam Pro, Segoe UI"/>
                        </Style>
                        <Style TargetType="DataGridRow">
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#F1F5F9"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#F0FDFA"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="12,0"/>
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="Foreground" Value="#1E293B"/>
                            <Setter Property="FontFamily" Value="Be Vietnam Pro, Segoe UI"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Foreground" Value="#0D9488"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã Đơn" Binding="{Binding OrderCode}" Width="90">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="#64748B"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Khách Hàng" Binding="{Binding Buyer.FullName}" Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Medium"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Tổng Tiền" Binding="{Binding TotalAmount, StringFormat={}{0:N0} đ}" Width="110">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="#0D9488"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Ngày Đặt" Binding="{Binding OrderDate, StringFormat=\{0:dd/MM/yyyy HH:mm\}}" Width="130">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#64748B"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTemplateColumn Header="Trạng Thái" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="5" Padding="8,2" HorizontalAlignment="Left" VerticalAlignment="Center">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding OrderStatus}" Value="Pending">
                                                        <Setter Property="Background" Value="#FFEFE2"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding OrderStatus}" Value="Shipping">
                                                        <Setter Property="Background" Value="#EFF6FF"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding OrderStatus}" Value="Completed">
                                                        <Setter Property="Background" Value="#ECFDF5"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding OrderStatus}" Value="Cancelled">
                                                        <Setter Property="Background" Value="#FEE2E2"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock FontSize="9.5" FontWeight="Bold" FontFamily="Be Vietnam Pro, Segoe UI" VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding OrderStatus}" Value="Pending">
                                                            <Setter Property="Text" Value="Chờ duyệt"/>
                                                            <Setter Property="Foreground" Value="#EA580C"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding OrderStatus}" Value="Shipping">
                                                            <Setter Property="Text" Value="Đang giao"/>
                                                            <Setter Property="Foreground" Value="#2563EB"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding OrderStatus}" Value="Completed">
                                                            <Setter Property="Text" Value="Đã xong"/>
                                                            <Setter Property="Foreground" Value="#059669"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding OrderStatus}" Value="Cancelled">
                                                            <Setter Property="Text" Value="Đã hủy"/>
                                                            <Setter Property="Foreground" Value="#EF4444"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="THAO TÁC" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Chi tiết" FontSize="11" Padding="8,4" Background="#0D9488" Foreground="White" BorderThickness="0" Cursor="Hand" Click="OpenDetail_Click" VerticalAlignment="Center">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Button.Template>
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Opacity" Value="0.9"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- ========================================== -->
        <!-- LIGHTBOX OVERLAY: ORDER DETAILS DIALOG -->
        <!-- ========================================== -->
        <Grid x:Name="LightboxOverlay" Visibility="Collapsed">
            <!-- Translucent Backdrop -->
            <Border Background="#990F172A" MouseDown="LightboxOverlay_MouseDown"/>

            <!-- Dialog Content Card -->
            <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Width="600" Margin="0,30,0,30">
                <Border Background="#FFFFFF" CornerRadius="16" Effect="{StaticResource CardShadow}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <!-- Header -->
                            <RowDefinition Height="Auto"/>
                            <!-- Scrollable Details -->
                            <RowDefinition Height="*"/>
                            <!-- Actions Footer -->
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Header -->
                        <Border Grid.Row="0" Background="#0D9488" CornerRadius="16,16,0,0" Padding="24,18">
                            <Grid>
                                <StackPanel>
                                    <TextBlock Text="{Binding SelectedOrder.OrderCode, StringFormat={}ĐƠN HÀNG: {0}}" FontSize="15" FontWeight="Bold" Foreground="White" FontFamily="Be Vietnam Pro, Segoe UI"/>
                                    <TextBlock Text="{Binding SelectedOrder.OrderDate, StringFormat={}Thời gian đặt: {0:dd/MM/yyyy HH:mm}}" FontSize="11" Foreground="#CCFFFFFF" Margin="0,4,0,0" FontFamily="Be Vietnam Pro, Segoe UI"/>
                                </StackPanel>
                                <Button HorizontalAlignment="Right" VerticalAlignment="Center" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="CloseLightbox_Click">
                                    <TextBlock Text="&#xE10A;" FontFamily="Segoe MDL2 Assets" FontSize="13" Foreground="White"/>
                                </Button>
                            </Grid>
                        </Border>

                        <!-- Details Body -->
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" MaxHeight="380" Padding="24,20">
                            <StackPanel>
                                <!-- Buyer info -->
                                <TextBlock Style="{StaticResource LabelHeader}" Text="Khách Hàng"/>
                                <TextBlock Style="{StaticResource ValueText}">
                                    <Run Text="{Binding SelectedOrder.Buyer.FullName, Mode=OneWay}" FontWeight="SemiBold"/>
                                    <Run Text=" - "/>
                                    <Run Text="{Binding SelectedOrder.Buyer.Email, Mode=OneWay}"/>
                                </TextBlock>

                                <!-- Address -->
                                <TextBlock Style="{StaticResource LabelHeader}" Text="Địa Chỉ Nhận Hàng"/>
                                <TextBlock Style="{StaticResource ValueText}">
                                    <Run Text="{Binding SelectedOrder.Address.RecipientName, Mode=OneWay}" FontWeight="Bold"/>
                                    <Run Text=" ("/>
                                    <Run Text="{Binding SelectedOrder.Address.Phone, Mode=OneWay}"/>
                                    <Run Text=")&#x0a;"/>
                                    <Run Text="{Binding SelectedOrder.Address.FullAddress, Mode=OneWay}"/>
                                </TextBlock>

                                <!-- Tracking code if shipping -->
                                <StackPanel>
                                    <StackPanel.Style>
                                        <Style TargetType="StackPanel">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding SelectedOrder.OrderStatus}" Value="Shipping">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding SelectedOrder.OrderStatus}" Value="Completed">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>
                                    <TextBlock Style="{StaticResource LabelHeader}" Text="Mã Vận Đơn (Giao hàng)"/>
                                    <TextBlock Style="{StaticResource ValueText}" Text="{Binding SelectedOrder.TrackingCode}" FontWeight="Bold" Foreground="#0D9488"/>
                                </StackPanel>

                                <!-- Notes -->
                                <TextBlock Style="{StaticResource LabelHeader}" Text="Ghi Chú Đơn Hàng"/>
                                <TextBlock Style="{StaticResource ValueText}" Text="{Binding SelectedOrder.Note}" FontStyle="Italic"/>

                                <!-- Separator -->
                                <Border BorderThickness="0,0,0,1" BorderBrush="#F1F5F9" Margin="0,4,0,14"/>

                                <!-- Order items list -->
                                <TextBlock Style="{StaticResource LabelHeader}" Text="Sản Phẩm Đã Đặt" Margin="0,0,0,10"/>
                                <ItemsControl ItemsSource="{Binding SelectedOrder.OrderDetails}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#F8FAFC" CornerRadius="8" Padding="12" Margin="0,0,0,8" BorderThickness="1" BorderBrush="#F1F5F9">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel Grid.Column="0">
                                                        <TextBlock Text="{Binding ProductNameSnapshot}" FontSize="12" FontWeight="SemiBold" Foreground="#1E293B" TextWrapping="Wrap" FontFamily="Be Vietnam Pro, Segoe UI"/>
                                                        <TextBlock Text="{Binding UnitPrice, StringFormat={}{0:N0} đ}" FontSize="10.5" Foreground="#64748B" Margin="0,4,0,0" FontFamily="Be Vietnam Pro, Segoe UI"/>
                                                    </StackPanel>
                                                    <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding Quantity, StringFormat={}x{0}}" FontSize="12.5" FontWeight="Bold" Foreground="#0D9488" HorizontalAlignment="Right" FontFamily="Be Vietnam Pro, Segoe UI"/>
                                                        <TextBlock Text="{Binding TotalPrice, StringFormat={}{0:N0} đ}" FontSize="11.5" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,4,0,0" FontFamily="Be Vietnam Pro, Segoe UI"/>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <!-- Separator -->
                                <Border BorderThickness="0,0,0,1" BorderBrush="#F1F5F9" Margin="0,8,0,14"/>

                                <!-- Bill amount stats -->
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Tạm tính:" FontSize="11.5" Foreground="#64748B" Margin="0,0,0,4" FontFamily="Be Vietnam Pro, Segoe UI"/>
                                        <TextBlock Text="Phí ship:" FontSize="11.5" Foreground="#64748B" Margin="0,0,0,6" FontFamily="Be Vietnam Pro, Segoe UI"/>
                                        <TextBlock Text="TỔNG THANH TOÁN:" FontSize="13" FontWeight="Bold" Foreground="#1E293B" FontFamily="Be Vietnam Pro, Segoe UI"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                        <TextBlock Text="{Binding SelectedOrder.SubTotal, StringFormat={}{0:N0} đ}" FontSize="11.5" Foreground="#1E293B" HorizontalAlignment="Right" Margin="0,0,0,4" FontFamily="Be Vietnam Pro, Segoe UI"/>
                                        <TextBlock Text="{Binding SelectedOrder.ShippingFee, StringFormat={}{0:N0} đ}" FontSize="11.5" Foreground="#1E293B" HorizontalAlignment="Right" Margin="0,0,0,6" FontFamily="Be Vietnam Pro, Segoe UI"/>
                                        <TextBlock Text="{Binding SelectedOrder.TotalAmount, StringFormat={}{0:N0} đ}" FontSize="15" FontWeight="Bold" Foreground="#0D9488" HorizontalAlignment="Right" FontFamily="Be Vietnam Pro, Segoe UI"/>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </ScrollViewer>

                        <!-- Actions Footer -->
                        <Border Grid.Row="2" BorderThickness="0,1,0,0" BorderBrush="#F1F5F9" Padding="24,16" CornerRadius="0,0,16,16">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Hủy đơn button -->
                                <Button Grid.Column="0" Command="{Binding CancelOrderCommand}" Click="CloseLightbox_Click" Background="#FEE2E2" BorderThickness="0" Height="36" Padding="16,0" Cursor="Hand" Margin="0,0,10,0">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                <TextBlock Text="Hủy Đơn Hàng" FontSize="12" Foreground="#EF4444" FontWeight="Bold" FontFamily="Be Vietnam Pro, Segoe UI" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Button.Template>
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding SelectedOrder.OrderStatus}" Value="Completed">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding SelectedOrder.OrderStatus}" Value="Cancelled">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <!-- Ship Order or Complete Order -->
                                <Button Grid.Column="1" Click="CloseLightbox_Click" BorderThickness="0" Height="36" Cursor="Hand">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Button.Template>
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Background" Value="#0D9488"/>
                                            <Setter Property="Content">
                                                <Setter.Value>
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="&#xE10B;" FontFamily="Segoe MDL2 Assets" FontSize="11" Foreground="#FFFFFF" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                                        <TextBlock Text=" Chuẩn Bị &amp; Giao Hàng" FontSize="12.5" Foreground="#FFFFFF" FontWeight="Bold" VerticalAlignment="Center" FontFamily="Be Vietnam Pro, Segoe UI"/>
                                                    </StackPanel>
                                                </Setter.Value>
                                            </Setter>
                                            <Setter Property="Command" Value="{Binding ShipOrderCommand}"/>
                                            
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding SelectedOrder.OrderStatus}" Value="Shipping">
                                                    <Setter Property="Background" Value="#2563EB"/>
                                                    <Setter Property="Content">
                                                        <Setter.Value>
                                                            <StackPanel Orientation="Horizontal">
                                                                <TextBlock Text="&#xE930;" FontFamily="Segoe MDL2 Assets" FontSize="11" Foreground="#FFFFFF" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                                                <TextBlock Text=" Giao Hàng Thành Công" FontSize="12.5" Foreground="#FFFFFF" FontWeight="Bold" VerticalAlignment="Center" FontFamily="Be Vietnam Pro, Segoe UI"/>
                                                            </StackPanel>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Setter Property="Command" Value="{Binding CompleteOrderCommand}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding SelectedOrder.OrderStatus}" Value="Completed">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding SelectedOrder.OrderStatus}" Value="Cancelled">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </Grid>
                        </Border>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
