<UserControl x:Class="TMDT.Views.Components.ProductCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="350" d:DesignWidth="220">
    <Border Background="{StaticResource CardBackgroundBrush}" 
            CornerRadius="12" 
            Effect="{StaticResource CardShadow}"
            Margin="10"
            Padding="0"
            RenderTransformOrigin="0.5,0.5"
            MouseLeftButtonUp="CardBorder_MouseLeftButtonUp"
            Cursor="Hand">
        <Border.RenderTransform>
            <TransformGroup>
                <ScaleTransform ScaleX="1" ScaleY="1" />
                <TranslateTransform X="0" Y="0" />
            </TransformGroup>
        </Border.RenderTransform>
        <Border.Triggers>
            <EventTrigger RoutedEvent="MouseEnter">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)" To="1.03" Duration="0:0:0.2">
                            <DoubleAnimation.EasingFunction>
                                <QuadraticEase EasingMode="EaseOut" />
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)" To="1.03" Duration="0:0:0.2">
                            <DoubleAnimation.EasingFunction>
                                <QuadraticEase EasingMode="EaseOut" />
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[1].(TranslateTransform.Y)" To="-5" Duration="0:0:0.2">
                            <DoubleAnimation.EasingFunction>
                                <QuadraticEase EasingMode="EaseOut" />
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
            <EventTrigger RoutedEvent="MouseLeave">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.2">
                            <DoubleAnimation.EasingFunction>
                                <QuadraticEase EasingMode="EaseOut" />
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.2">
                            <DoubleAnimation.EasingFunction>
                                <QuadraticEase EasingMode="EaseOut" />
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[1].(TranslateTransform.Y)" To="0" Duration="0:0:0.2">
                            <DoubleAnimation.EasingFunction>
                                <QuadraticEase EasingMode="EaseOut" />
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Border.Triggers>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="180"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Product Image Area -->
            <Border Grid.Row="0" CornerRadius="12,12,0,0" Background="#F1F5F9" Margin="5">
                <Grid>
                    <materialDesign:PackIcon Kind="ImageOutline" Width="50" Height="50" 
                                             HorizontalAlignment="Center" VerticalAlignment="Center"
                                             Foreground="{StaticResource TextMutedBrush}"
                                             Visibility="{Binding Product.MainImageUrl, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Inverse}"/>
                    <Image Source="{Binding Product.MainImageUrl}" Stretch="Uniform" Margin="2"/>
                    <!-- Heart Icon -->
                    <ToggleButton x:Name="HeartToggle"
                                  Style="{StaticResource MaterialDesignActionToggleButton}"
                                  HorizontalAlignment="Right" VerticalAlignment="Top"
                                  Margin="5" Width="30" Height="30"
                                  Background="Transparent" BorderThickness="0"
                                  IsChecked="{Binding IsWishlisted, Mode=TwoWay}"
                                  Click="HeartToggle_Click"
                                  RenderTransformOrigin="0.5,0.5">
                        <ToggleButton.RenderTransform>
                            <ScaleTransform ScaleX="1" ScaleY="1" />
                        </ToggleButton.RenderTransform>
                        <ToggleButton.Triggers>
                            <EventTrigger RoutedEvent="ToggleButton.Checked">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                                                         From="1.0" To="1.4" Duration="0:0:0.15" AutoReverse="True" />
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                                                         From="1.0" To="1.4" Duration="0:0:0.15" AutoReverse="True" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ToggleButton.Triggers>
                        <ToggleButton.Content>
                            <materialDesign:PackIcon Kind="HeartOutline" />
                        </ToggleButton.Content>
                        <materialDesign:ToggleButtonAssist.OnContent>
                            <materialDesign:PackIcon Kind="Heart" Foreground="Red" />
                        </materialDesign:ToggleButtonAssist.OnContent>
                    </ToggleButton>
                </Grid>
            </Border>

            <!-- Product Info -->
            <StackPanel Grid.Row="1" Margin="15,10,15,15">
                <TextBlock Text="{Binding Product.ProductName}" 
                           FontSize="14" FontWeight="Medium" 
                           Foreground="{StaticResource TextBrush}"
                           TextTrimming="CharacterEllipsis"
                           Margin="0,0,0,5"/>

                <!-- Shop Name -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <materialDesign:PackIcon Kind="StorefrontOutline" Width="14" Height="14" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" Margin="0,0,4,0"/>
                    <TextBlock Text="{Binding Product.Shop.ShopName, TargetNullValue='Không có tên'}" FontSize="12" Foreground="{StaticResource TextMutedBrush}" TextTrimming="CharacterEllipsis"/>
                </StackPanel>

                <!-- Price Row -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBlock Text="{Binding Product.Price, StringFormat={}{0:N0}đ}" 
                               FontSize="16" FontWeight="Bold" 
                               Foreground="{StaticResource PriceBrush}"
                               Margin="0,0,10,0"/>
                    <TextBlock Text="{Binding Product.OriginalPrice, StringFormat={}{0:N0}đ}" 
                               FontSize="12" Foreground="{StaticResource TextMutedBrush}"
                               TextDecorations="Strikethrough"
                               VerticalAlignment="Bottom"/>
                </StackPanel>

                <!-- Rating -->
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Star" Foreground="{StaticResource RatingBrush}" Width="15" Height="15"/>
                    <materialDesign:PackIcon Kind="Star" Foreground="{StaticResource RatingBrush}" Width="15" Height="15"/>
                    <materialDesign:PackIcon Kind="Star" Foreground="{StaticResource RatingBrush}" Width="15" Height="15"/>
                    <materialDesign:PackIcon Kind="Star" Foreground="{StaticResource RatingBrush}" Width="15" Height="15"/>
                    <materialDesign:PackIcon Kind="StarHalf" Foreground="{StaticResource RatingBrush}" Width="15" Height="15"/>
                    <TextBlock Text="{Binding Product.SoldCount, TargetNullValue=0, StringFormat='({0} đã bán)'}" FontSize="12" Foreground="{StaticResource TextMutedBrush}" Margin="5,0,0,0"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
