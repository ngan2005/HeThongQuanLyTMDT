<Window x:Class="TMDT.Views.Components.VariantSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Chọn Biến Thể Sản Phẩm" Height="400" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}">
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Chọn loại sản phẩm" FontWeight="Bold" FontSize="18" Grid.Row="0" Margin="0,0,0,10"/>
        
        <ListBox x:Name="lstVariants" Grid.Row="1" ItemsSource="{Binding}" 
                 HorizontalContentAlignment="Stretch" SelectionChanged="lstVariants_SelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,4">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding VariantName}" FontWeight="Bold" FontSize="15"/>
                                <TextBlock Text="{Binding Sku, StringFormat='Mã: {0}'}" FontSize="12" Foreground="Gray"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <TextBlock Text="{Binding ExtraPrice, StringFormat='+ {0:N0} đ'}" Foreground="Green" FontWeight="Medium"/>
                                <TextBlock Text="{Binding Quantity, StringFormat='Kho: {0}'}" Foreground="Gray" FontSize="12" TextAlignment="Right"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="btnCancel" Content="Hủy" Style="{StaticResource MaterialDesignOutlinedButton}" Width="100" Margin="0,0,10,0" Click="btnCancel_Click"/>
            <Button x:Name="btnConfirm" Content="Xác nhận" Width="100" Click="btnConfirm_Click"/>
        </StackPanel>
    </Grid>
</Window>
